---
const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="formatted_time">
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
